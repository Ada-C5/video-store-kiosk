<h2>Much Wow</h2>

<table>
  {{#each model as |movies|}}
    <td>
      {{movies.title}}
      <img src="http://localhost:3000/images/posters/{{movies.poster_filename}}">
    </td>
  {{/each}}
</table>

<button {{action "clickPrev"}}> Previous </button>
<button {{action "clickNext"}}> Next </button>

{{outlet}}
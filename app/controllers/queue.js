import Ember from 'ember';

export default Ember.Controller.extend({
  // model: function(params) {
  // parentModel = this.modelFor('parent');
  // return {queue: parentModel.get('queue')};
  // }
});

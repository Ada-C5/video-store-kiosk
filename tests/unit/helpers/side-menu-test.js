import { sideMenu } from 'video-store-kiosk/helpers/side-menu';
import { module, test } from 'qunit';

module('Unit | Helper | side menu');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sideMenu([42]);
  assert.ok(result);
});
